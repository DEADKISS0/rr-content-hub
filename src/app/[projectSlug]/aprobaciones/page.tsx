import { getCurrentUser, getIdeas, getProject } from '@/lib/data'; import { notFound, redirect } from 'next/navigation'; import { WorkspaceSection } from '@/components/workspace-section';
export default async function Approvals({params}:{params:Promise<{projectSlug:string}>}){const {projectSlug}=await params;const {user}=await getCurrentUser();if(!user)redirect('/login');const {project}=await getProject(projectSlug);if(!project)notFound();const ideas=(await getIdeas(project.id)).filter((i:any)=>['pending_approval','needs_changes','ready_to_publish'].includes(i.status));return <WorkspaceSection title="APROBACIONES" eyebrow={`${project.name} · CLIENT_REVIEW`} description="Revisa conceptos y versiones antes de desbloquear producción o publicación. Cada decisión debe quedar acompañada por una evidencia." ideas={ideas} projectSlug={projectSlug} action="REVISAR TODO"/>}
