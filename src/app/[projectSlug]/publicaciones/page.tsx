import { getCurrentUser, getIdeas, getProject } from '@/lib/data'; import { notFound, redirect } from 'next/navigation'; import { WorkspaceSection } from '@/components/workspace-section';
export default async function Publications({params}:{params:Promise<{projectSlug:string}>}){const {projectSlug}=await params;const {user}=await getCurrentUser();if(!user)redirect('/login');const {project}=await getProject(projectSlug);if(!project)notFound();const ideas=await getIdeas(project.id);return <WorkspaceSection title="PUBLICACIONES" eyebrow={`${project.name} · RELEASE_QUEUE`} description="Controla qué sale, cuándo sale, en qué plataforma y con qué copy. Orgánico y pauta viven en un mismo calendario operativo." ideas={ideas} projectSlug={projectSlug} action="PROGRAMAR"/>}
