import { getIdeas, getProject } from '@/lib/data'; import { notFound } from 'next/navigation'; import { WorkspaceSection } from '@/components/workspace-section';
export default async function Metrics({params}:{params:Promise<{projectSlug:string}>}){const {projectSlug}=await params;const {project}=await getProject(projectSlug);if(!project)notFound();const ideas=await getIdeas(project.id);return <WorkspaceSection title="MÉTRICAS" eyebrow={`${project.name} · PERFORMANCE_LOOP`} description="Conecta cada publicación con su hipótesis, objetivo y resultado. La medición cierra el ciclo para decidir qué repetir y qué descartar." ideas={ideas.filter((i:any)=>i.status==='published')} projectSlug={projectSlug} action="REGISTRAR RESULTADO"/>}
