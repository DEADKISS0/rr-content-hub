import { getProject } from '@/lib/data'; import { notFound, redirect } from 'next/navigation'; import { WorkspaceShell } from '@/components/workspace-shell';
export default async function ProjectLayout({children,params}:{children:React.ReactNode;params:Promise<{projectSlug:string}>}){const {projectSlug}=await params;const {project,access}=await getProject(projectSlug);if(!project)notFound();if(!access)redirect('/select-project');return <WorkspaceShell project={project} role={access.role_in_project}>{children}</WorkspaceShell>}
