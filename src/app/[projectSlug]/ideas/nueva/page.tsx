'use client';
import { useState } from 'react'; import { useParams, useRouter } from 'next/navigation';
export default function NewIdea(){const router=useRouter();const {projectSlug}=useParams<{projectSlug:string}>();const [saved,setSaved]=useState(false);return <main className="min-h-screen bg-negro"><header className="border-b-2 border-blanco px-5 py-4 md:px-10"><div className="mx-auto flex max-w-6xl justify-between"><button onClick={()=>router.back()} className="font-mono text-xs text-blanco-60">← VOLVER</button><span className="mono-label text-mostaza">[NEW_CONTENT_RECORD]</span></div></header><div className="mx-auto max-w-4xl px-5 py-10 md:px-10"><p className="eyebrow">{projectSlug} · CAPTURA</p><h1 className="display-title">NUEVA<br/><em>IDEA.</em></h1><form onSubmit={e=>{e.preventDefault();setSaved(true)}} className="mt-10 space-y-6"><Field label="TÍTULO" placeholder="Ej. La textura que se siente"/><div className="grid gap-6 md:grid-cols-2"><Field label="TIPO" select options={['Orgánico','Pauta']}/><Field label="CATEGORÍA" placeholder="Producto y tela"/></div><Field label="OBJETIVO" textarea placeholder="¿Qué debe conseguir esta pieza?"/><Field label="DESCRIPCIÓN / CONCEPTO" textarea placeholder="Describe la idea en lenguaje claro..."/><div className="grid gap-6 md:grid-cols-3"><Field label="CÁMARA" textarea placeholder="Especificaciones"/><Field label="TALENTO" textarea placeholder="Modelo / perfil"/><Field label="EDICIÓN" textarea placeholder="Ritmo / audio"/></div><button className="btn-brutal" type="submit">{saved?'GUARDADO ✓':'GUARDAR BORRADOR →'}</button></form></div></main>}
function Field({label,placeholder,textarea,select,options}:{label:string;placeholder?:string;textarea?:boolean;select?:boolean;options?:string[]}){return <label className="block"><span className="mono-label mb-2 block text-mostaza">// {label}</span>{select?<select className="input-brutal">{options?.map(o=><option key={o}>{o}</option>)}</select>:textarea?<textarea className="input-brutal min-h-28" placeholder={placeholder}/>:<input className="input-brutal" placeholder={placeholder}/>}</label>}
