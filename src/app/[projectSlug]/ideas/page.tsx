import { getIdeas, getProject } from '@/lib/data'; import { notFound } from 'next/navigation'; import Link from 'next/link'; import { IdeasBoard } from '@/components/ideas-board';
export default async function IdeasList({params}:{params:Promise<{projectSlug:string}>}){const {projectSlug}=await params;const {project}=await getProject(projectSlug);if(!project)notFound();const ideas:any[]=await getIdeas(project.id);return <main className="min-h-screen bg-negro"><header className="border-b-2 border-blanco px-5 py-4 md:px-10"><div className="mx-auto flex max-w-7xl items-center justify-between"><Link href={`/${projectSlug}`} className="font-mono text-xs text-blanco-60 hover:text-mostaza">← DASHBOARD</Link><h1 className="font-display text-2xl font-bold text-blanco">BANCO_DE_IDEAS</h1><Link href={`/${projectSlug}/ideas/nueva`} className="btn-brutal">+ NUEVA</Link></div></header><div className="mx-auto max-w-7xl px-5 py-8 md:px-10"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">{project.name} · CONTENT_PIPELINE</p><h2 className="display-title">IDEAS<br/><em>EN MARCHA.</em></h2></div><p className="font-mono text-xs text-blanco-60">{ideas.length} registros · orgánico + pauta</p></div><IdeasBoard ideas={ideas} projectSlug={projectSlug}/></div></main>}
