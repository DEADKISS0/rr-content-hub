import { getCurrentUser, getIdeas, getProject } from '@/lib/data'; import { notFound, redirect } from 'next/navigation'; import { WorkspaceSection } from '@/components/workspace-section';
export default async function Production({params}:{params:Promise<{projectSlug:string}>}){const {projectSlug}=await params;const {user}=await getCurrentUser();if(!user)redirect('/login');const {project}=await getProject(projectSlug);if(!project)notFound();const ideas=await getIdeas(project.id);return <WorkspaceSection title="PRODUCCIÓN" eyebrow={`${project.name} · PRODUCTION_BOARD`} description="Todo lo que ya tiene dirección aprobada y necesita convertirse en una pieza lista para publicar. Cada rol trabaja desde su propio brief." ideas={ideas} projectSlug={projectSlug} action="NUEVA TAREA"/>}
