import { getProjects } from '@/lib/data';
import Link from 'next/link';

export default async function SelectProject() {
  const { projects } = await getProjects(); const access: any = projects[0]; const project = access?.projects ?? access;
  if (!project) return <main className="min-h-screen bg-negro px-5 py-20"><div className="mx-auto max-w-3xl border-2 border-mostaza p-8"><p className="eyebrow">[WUNDEER]</p><h1 className="mt-4 font-display text-4xl font-bold text-blanco">SIN DATOS DISPONIBLES.</h1></div></main>;
  return <main className="min-h-screen bg-negro px-5 py-10 md:px-12"><div className="mx-auto max-w-5xl"><header className="mb-14 border-b-2 border-blanco pb-10"><p className="eyebrow">[RR CONTENT HUB · ESPACIO ÚNICO]</p><h1 className="display-title">WUNDEER<br/><em>EN MARCHA.</em></h1><p className="mt-6 max-w-xl text-base leading-8 text-blanco-60">Aquí vive toda la operación de contenido. Una sola marca, un solo tablero, un siguiente paso claro.</p></header><Link href="/wundeer" className="group block border-2 border-fucsia bg-fucsia/10 p-7 shadow-brutal-lg transition-transform duration-300 hover:-translate-y-1 md:p-10"><p className="mono-label text-mostaza">[PROYECTO ACTIVO · {access.role_in_project ?? 'OWNER'}]</p><div className="mt-6 flex flex-wrap items-end justify-between gap-6"><div><h2 className="font-display text-5xl font-bold text-blanco md:text-7xl">{project.name}</h2><p className="mt-3 font-mono text-xs text-blanco-60">{project.client_name}</p></div><span className="font-mono text-sm text-mostaza">ABRIR MAPA →</span></div></Link><p className="mt-8 font-mono text-[10px] text-blanco-40">WUNDEER ES EL ÚNICO PROYECTO VISIBLE EN ESTE HUB.</p></div></main>;
}
