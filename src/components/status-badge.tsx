export function StatusBadge({ status }: { status: string }) { const labels: Record<string,string> = { draft:'BORRADOR', pending_approval:'PENDIENTE', approved:'APROBADO', needs_changes:'AJUSTES', in_production:'PRODUCCIÓN', editing:'EDICIÓN', ready_to_publish:'LISTO', published:'PUBLICADO' }; return <span className={`status status-${status}`}>[{labels[status] ?? status}]</span>; }
